<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/BaseTheme">
        <item name="android:colorPrimaryDark">@color/primaryDark</item>
        <item name="android:navigationBarColor">@color/primaryDarkest</item>
    </style>

</resources>
